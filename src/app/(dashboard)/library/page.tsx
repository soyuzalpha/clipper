import { PageStub } from "@/components/page-stub";

export default function LibraryPage() {
  return (
    <PageStub
      title="Content Library"
      description="All assets — videos, clips, thumbnails, scripts, and captions."
      emptyTitle="Library is empty"
      emptyDescription="Assets created during production will appear here."
    />
  );
}
