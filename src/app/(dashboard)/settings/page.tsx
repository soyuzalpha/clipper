import { PageStub } from "@/components/page-stub";

export default function SettingsPage() {
  return (
    <PageStub
      title="Settings"
      description="Workspace configuration and AI provider keys."
      emptyTitle="Settings coming soon"
      emptyDescription="Configure AI providers and workspace preferences."
    />
  );
}
