import { PageStub } from "@/components/page-stub";

export default function PlannerPage() {
  return (
    <PageStub
      title="Planner"
      description="Calendar, board, and list views for scheduling content."
      emptyTitle="Nothing planned yet"
      emptyDescription="Plan content from your ideas to see it on the calendar."
    />
  );
}
