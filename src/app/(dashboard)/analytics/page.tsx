import { PageStub } from "@/components/page-stub";

export default function AnalyticsPage() {
  return (
    <PageStub
      title="Analytics"
      description="Performance across platforms, hooks, topics, and campaigns."
      emptyTitle="No analytics yet"
      emptyDescription="Publish content to start tracking performance."
    />
  );
}
