import { PageStub } from "@/components/page-stub";

export default function ProductionPage() {
  return (
    <PageStub
      title="Production"
      description="Workspace for turning selected ideas into finished content."
      emptyTitle="No active production"
      emptyDescription="Select an idea to start producing content."
    />
  );
}
